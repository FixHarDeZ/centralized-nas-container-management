users:
  admin:
    displayname: "Admin"
    password: "$argon2id$REPLACE_WITH_HASH — run: docker run --rm authelia/authelia:latest authelia crypto hash generate argon2 --password 'YOUR_PASSWORD'"
    email: "admin@example.com"
    groups:
      - admins
      - users
