# AI Deck implementation plan

**Goal:** Show accurate Codex login state, select chat model/effort, and rename the stack to `ai-deck` without deploying or losing existing data.

**Architecture:** Keep the stdlib HTTP services and browser client. Query CLI authentication without exposing its output; publish a provider-specific model catalog and validate selections before spawning a turn. Restart idle Claude with its session ID when options change. Rename runtime references, keeping explicit legacy persistence identities.

**Constraints:** Work on the existing uncommitted AI Desk implementation in place. Do not discard or stage unrelated work. No deployment, real login, paid turn, credential output, or data-volume deletion. Existing NAS port 5072 and work bind remain stable.

- [x] Add failing tests for signed-in/signed-out/unknown auth, model/effort validation, subprocess arguments, and Claude resume on option changes.
- [x] Implement `GET /api/auth`, `GET /chat/options`, validated send options, and safe CLI argument arrays. Auth errors remain unknown rather than claiming logged out.
- [x] Add browser selectors, per-provider preferences, login polling on focus/visibility and visible unknown/loading states. Check desktop and phone controls with the real browser harness.
- [x] Rename directory, Compose project/services/containers, internal image paths, UI branding, deploy registry, updater/vendor scripts and active documentation to `ai-deck` / AI Deck.
- [x] Keep legacy home volume explicitly named and external, preserve work-path environment compatibility and stored browser preferences, and document one-time migration/rollback separately from routine deploy.
- [x] Run all stack tests, relevant repo tests, syntax checks, Compose rendering and image/container checks where available; record exact limitations.
- [x] Update `ai-deck/.notes/00_INDEX.md` and `daily_log.md`, and deliver the changes plus a migration runbook. Do not deploy.

## Validation and migration design

`pytest ai-deck/tests` must verify auth redaction, invalid options, busy rejection, CLI argv, continuing a Claude session, provider isolation, and browser layout. Repository reference searches must find no active reference to the removed source directory outside intentional legacy identities/history. Compose must use `name: ai-deck` with an external volume defaulting to `claude-desk_claude_desk_home`; the operator verifies its actual name before cutover and can override `AI_DECK_HOME_VOLUME`. Existing work directory remains `${CLAUDE_WORK_DIR}` so encrypted secrets need no migration. The future cutover stops the old Compose project without `-v`, deploys the new directory, validates login/history/files, and retains the old directory for rollback.

Completed locally on 2026-09-20. Validation boundaries: `ai-deck/.notes/00_INDEX.md`. No deployment, commit or push.
