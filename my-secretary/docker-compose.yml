services:
  my-secretary:
    build: .
    container_name: my-secretary
    restart: unless-stopped
    ports:
      - "5057:8000"
    env_file: .env
    environment:
      - TZ=Asia/Bangkok
    volumes:
      - my_secretary_data:/data

volumes:
  my_secretary_data:
